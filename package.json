{
  "name": "model-viewer",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:e2e": "bun run build && bunx playwright test",
    "test:e2e:headed": "bun run build && bunx playwright test --headed",
    "build-catalog": "bun run scripts/build-item-catalog.ts"
  },
  "dependencies": {
    "@wowserhq/format": "^0.28.0",
    "@wowserhq/stormjs": "^0.4.1",
    "three": "^0.182.0",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.75.0",
    "@playwright/test": "^1.58.2",
    "@types/three": "^0.182.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}